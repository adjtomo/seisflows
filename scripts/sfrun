#!/usr/bin/env python

from seisflows.tools.config import ConfigObj, ParameterObj


# The ParameterObj utility reads parameter files and stores each file's contents
# in a globally accessible dictionary.
ParameterObj('SeisflowsParameters').init('parameters')
ParameterObj('SeisflowsPaths').init('paths')


# SeisFlows consists of interacting 'system', 'preprocess', 'solver', 
# 'postprocess', 'optimize', and 'workflow' objects. The ConfigObj utility 
# initializes these objects and makes them accessible through the standard 
# Python import system.
ConfigObj('SeisflowsObjects').init()

import system
import workflow


# Now all that remains is to execute the workflow. Depending on system details,
# the workflow may be executed immediately or passed to a scheduler.
system.submit(workflow)

