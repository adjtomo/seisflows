#!/usr/bin/env python

from seisflows.tools.config import SeisflowsObjects, SeisflowsParameters, SeisflowsPaths


# The SeisflowsParameters and SeisflowsPaths utilities read parameter files 
# and stores each file's contents in a globally accessible dictionary.
SeisflowsParameters().initialize()
SeisflowsPaths().initialize()


# SeisFlows consists of interacting 'system', 'preprocess', 'solver', 
# 'postprocess', 'optimize', and 'workflow' objects. The SeisflowObjects utility
# initializes these objects and makes them accessible through the standard 
# Python import system.
SeisflowsObjects().initialize()

import system
import workflow


# Now all that remains is to execute the workflow. Depending on system details,
# the workflow may be executed immediately or passed to a scheduler.
system.submit(workflow)


