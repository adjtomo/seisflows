name: seisflows
channels:
    - conda-forge
dependencies:
    - pip
    - obspy
    - cartopy
    - pyyaml
    - pypdf
    - pip:
      #- pyatoa>=0.4.0
      - git+https://github.com/adjtomo/pyatoa.git@devel
      - pysep-adjtomo
      - -e .

